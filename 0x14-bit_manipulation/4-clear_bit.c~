#include "main.h"

/**
 * clear_bit - sets the value of a bit at index to 0
 * @index: the loction of the bit to be changed
 * @n: the bit to change
 * Return: 1 if successful and -1 if unsuccessful
 */
int clear_bit(unsigned long int *n, unsigned int index)
  
