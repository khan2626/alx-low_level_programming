#include "main.h"

/**
 * print_last_digit - print last digit of a number
 * @n: the number
 * Return: the last digit
 */
int print_last_digit(int n)
{
	int ld = n % 10;

	if (ld < o)
		ld = -1;

	_putchar(ld + '0');
	return (0);
}
